RewriteEngine On

# Handle API requests
RewriteRule ^api/(.*)$ index.php/api/$1 [L]

# Handle all other requests through index.php (for SPA routing)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php/$1 [L]